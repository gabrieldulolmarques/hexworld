services:
  server:
    init: true
    build: ./server
    ports:
      - "5000:5000"
    environment:
      DATABASE_PATH: /app/data/hexworld.db
      SERVER_ADDRESS: 0.0.0.0:5000
      PYTHONUNBUFFERED: 1
    volumes:
      - ./server:/app
