services:
  server:
    init: true
    build: ./server
    ports:
      - "5000:5000"
    environment:
      DB_PATH: /app/data/hexworld.db
      PYTHONUNBUFFERED: 1
    volumes:
      - ./server:/app