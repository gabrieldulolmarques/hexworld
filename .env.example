# UID/GID do host — evita que server/data/ fique owned by root no volume Docker
UID=1000
GID=1000

# Cliente — make up
SERVER_ADDRESS=127.0.0.1:9090
CALLBACK_ADDRESS=0.0.0.0:9092
CALLBACK_PUBLIC_ADDRESS=host.docker.internal:9092
